{# templates/pdf/equipements.html.twig #}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Équipements du client</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
        }
        .page-break {
            page-break-after: always;
        }
        .equipement {
            margin-bottom: 20px;
            border: 1px solid #ddd;
            padding: 10px;
        }
        .equipement-header {
            background-color: #f5f5f5;
            padding: 5px;
            margin-bottom: 10px;
            font-weight: bold;
        }
        .photo {
            max-width: 300px;
            max-height: 200px;
            margin: 5px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 5px;
            text-align: left;
        }
        th {
            width: 30%;
            background-color: #f9f9f9;
        }
        .filter-info {
            background-color: #e7f3ff;
            border: 1px solid #b3d9ff;
            border-radius: 4px;
            padding: 10px;
            margin: 10px 0;
            font-size: 11px;
        }
        .filter-badge {
            background-color: #007bff;
            color: white;
            padding: 3px 8px;
            border-radius: 3px;
            font-size: 10px;
            margin-right: 5px;
        }
        
        /* Styles pour le tableau de statistiques */
        .stats-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-size: 11px;
        }
        .stats-table th {
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
            text-align: center;
            padding: 8px;
        }
        .stats-table td {
            text-align: center;
            padding: 6px;
            border: 1px solid #ddd;
        }
        .stats-table .statut-cell {
            text-align: left;
            padding-left: 15px;
        }
        
        /* Logos de statut */
        .logo-statut {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: inline-block;
            margin-right: 8px;
            vertical-align: middle;
        }
        .logo-vert { background-color: #4CAF50; }
        .logo-orange { background-color: #FF9800; }
        .logo-rouge { background-color: #F44336; }
        .logo-noir { background-color: #424242; }
        
        .stats-section {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 15px;
            margin: 20px 0;
        }
        .stats-title {
            font-size: 14px;
            font-weight: bold;
            margin-bottom: 15px;
            color: #495057;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Rapport d'équipements</h1>
        <span class="agence-badge">Agence {{ agence }}</span>
        
        {% if isFiltered is defined and isFiltered %}
            <div class="filter-info">
                <strong>Filtres appliqués :</strong>
                {% if clientAnneeFilter %}
                    <span class="filter-badge">Année : {{ clientAnneeFilter }}</span>
                {% endif %}
                {% if clientVisiteFilter %}
                    <span class="filter-badge">Visite : {{ clientVisiteFilter }}</span>
                {% endif %}
            </div>
        {% endif %}
    </div>
    
    <div class="summary">
        <h3>Résumé</h3>
        <p><strong>Nombre total d'équipements :</strong> {{ equipmentsWithPictures|length }}</p>
        {% if isFiltered is defined and isFiltered %}
            <p><em>Résultats filtrés selon les critères sélectionnés</em></p>
        {% else %}
            <p><em>Tous les équipements du client</em></p>
        {% endif %}
    </div>
    
    {# Tableau de statistiques #}
    {% if statistiques is defined %}
    <div class="stats-section">
        <div class="stats-title">📊 Statistiques par statut</div>
        
        <table class="stats-table">
            <thead>
                <tr>
                    <th>Statut</th>
                    <th>État</th>
                    <th>Total</th>
                    <th>Total inexistant</th>
                </tr>
            </thead>
            <tbody>
                {% for etat, count in statistiques.etatsCount %}
                    {% set logoColor = '' %}
                    {% if etat == "Bon état de fonctionnement le jour de la visite" %}
                        {% set logoColor = 'vert' %}
                    {% elseif etat == "Travaux préventifs" %}
                        {% set logoColor = 'orange' %}
                    {% elseif etat in ["Travaux curatifs", "Equipement à l'arrêt le jour de la visite", "Equipement mis à l'arrêt lors de l'intervention"] %}
                        {% set logoColor = 'rouge' %}
                    {% elseif etat in ["Equipement inaccessible le jour de la visite", "Equipement non présent sur site"] %}
                        {% set logoColor = 'noir' %}
                    {% endif %}
                    
                    <tr>
                        <td style="text-align: center;">
                            <img src="https://www.backend-kizeo.somafi-group.fr/public/img/logos/{{ logoColor }}.png" 
                                 alt="logo {{ logoColor }}" 
                                 style="width: 20px; height: 20px;">
                        </td>
                        <td style="text-align: left; padding-left: 10px;">{{ etat }}</td>
                        <td style="text-align: center;">
                            {% if etat == "Equipement non présent sur site" %}
                                {# Pour les équipements non présents, on n'affiche pas le total dans la colonne Total #}
                            {% else %}
                                {{ count }}
                            {% endif %}
                        </td>
                        <td style="text-align: center;">
                            {% if etat == "Equipement non présent sur site" %}
                                {{ count }}
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        
        {# Légende des couleurs #}
        <div style="margin-top: 15px; font-size: 10px;">
            <strong>Légende :</strong>
            <img src="https://www.backend-kizeo.somafi-group.fr/public/img/logos/vert.png" alt="vert" style="width: 15px; height: 15px; vertical-align: middle;"> Bon état |
            <img src="https://www.backend-kizeo.somafi-group.fr/public/img/logos/orange.png" alt="orange" style="width: 15px; height: 15px; vertical-align: middle;"> Travaux préventifs |
            <img src="https://www.backend-kizeo.somafi-group.fr/public/img/logos/rouge.png" alt="rouge" style="width: 15px; height: 15px; vertical-align: middle;"> Travaux curatifs/Arrêt |
            <img src="https://www.backend-kizeo.somafi-group.fr/public/img/logos/noir.png" alt="noir" style="width: 15px; height: 15px; vertical-align: middle;"> Inaccessible/Inexistant
        </div>
    </div>
    {% endif %}
    
    {% for equipmentData in equipmentsWithPictures %}
        {% set equipment = equipmentData.equipment %}
        {% set pictures = equipmentData.pictures %}
        
        <div class="equipement">
            <div class="equipement-header">
                <div class="equipement-title">
                    Équipement {{ equipment.numeroEquipement }} - {{ equipment.raisonSociale }}
                    {% if equipment.etat %}
                        {% set logoColor = '' %}
                        {% if equipment.etat == "Bon état de fonctionnement le jour de la visite" %}
                            {% set logoColor = 'vert' %}
                        {% elseif equipment.etat == "Travaux préventifs" %}
                            {% set logoColor = 'orange' %}
                        {% elseif equipment.etat in ["Travaux curatifs", "Equipement à l'arrêt le jour de la visite", "Equipement mis à l'arrêt lors de l'intervention"] %}
                            {% set logoColor = 'rouge' %}
                        {% elseif equipment.etat in ["Equipement inaccessible le jour de la visite", "Equipement non présent sur site"] %}
                            {% set logoColor = 'noir' %}
                        {% endif %}
                        <img src="https://www.backend-kizeo.somafi-group.fr/public/img/logos/{{ logoColor }}.png" 
                             alt="logo {{ logoColor }}" 
                             style="width: 20px; height: 20px; float: right; margin-top: 2px;">
                    {% endif %}
                </div>
            </div>
            
            <h4>Informations techniques</h4>
            <table class="info-table">
                <tr>
                    <th>Numéro</th>
                    <td>{{ equipment.numeroEquipement }}</td>
                    <th>Type</th>
                    <td>{{ equipment.libelleEquipement ?? 'Non renseigné' }}</td>
                </tr>
                <tr>
                    <th>Marque</th>
                    <td>{{ equipment.marque ?? 'Non renseigné' }}</td>
                    <th>Modèle</th>
                    <td>{{ equipment.modele ?? 'Non renseigné' }}</td>
                </tr>
                <tr>
                    <th>Date enregistrement</th>
                    <td>{{ equipment.dateEnregistrement ? equipment.dateEnregistrement|date('d/m/Y') : 'Non renseigné' }}</td>
                    <th>Dernière visite</th>
                    <td>{{ equipment.derniereVisite ? equipment.derniereVisite|date('d/m/Y') : 'Aucune' }}</td>
                </tr>
                {% if equipment.etat %}
                <tr>
                    <th>État</th>
                    <td colspan="3">{{ equipment.etat }}</td>
                </tr>
                {% endif %}
            </table>
            
            <div class="photos-section">
                <h4>Photos ({{ pictures|length }} photo{{ pictures|length > 1 ? 's' : '' }})</h4>
                {% if pictures|length > 0 %}
                    <div class="photos-grid">
                        {% for pictureData in pictures %}
                            <div class="photo-container">
                                <img src="data:image/jpeg;base64,{{ pictureData.picture }}" class="photo" alt="Photo équipement">
                                <div class="photo-caption">
                                    {{ pictureData.update_time ? pictureData.update_time|date('d/m/Y H:i') : 'Date inconnue' }}
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <p><em>Aucune photo disponible pour cet équipement.</em></p>
                {% endif %}
            </div>
        </div>
        
        {% if not loop.last %}
            <div class="page-break"></div>
        {% endif %}
    {% endfor %}
    
    <div style="margin-top: 30px; font-size: 9px; color: #666; text-align: center; border-top: 1px solid #ddd; padding-top: 10px;">
        Document généré automatiquement le {{ "now"|date_modify("+2 hours")|date('d/m/Y à H:i') }} - {{ equipmentsWithPictures|length }} équipement{{ equipmentsWithPictures|length > 1 ? 's' : '' }} traité{{ equipmentsWithPictures|length > 1 ? 's' : '' }}
        {% if isFiltered is defined and isFiltered %}
            <br><em>Filtres appliqués lors de la génération</em>
        {% endif %}
    </div>
</body>
</html>